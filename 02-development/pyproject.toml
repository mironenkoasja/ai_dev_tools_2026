[project]
name = "fairshare-backend"
version = "0.1.0"
description = "FairShare backend API"
requires-python = ">=3.11"
dependencies = [
  "Django>=5.1,<6.0",
  "SQLAlchemy>=2.0,<3.0",
]

[tool.uv]
package = false
